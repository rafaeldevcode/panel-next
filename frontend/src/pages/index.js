export async function getStaticProps(){
    return {
        redirect: {
            destination: '/admin/login',
            permanent: false,
        }
    }

    return {
        props: {}
    }
}

export default function Index(){
    return
}
